spring:
  datasource:
    url: dbc:h2:mem:test
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        # show_sql: true
        format_sql: true

logging.level:
   org.hibernate.SQL: debug
   org.hibernate.type: trace  # 쿼리 파라미터 남김
   
   #메모리 모드로 실행(테스트시)